<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.danieljayarajan.xmlrecipeapp">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XMLRecipeApp">
        <activity android:name=".activities.baseActivities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.recipeActivities.RecipeTypeBaseActivity" />
        <activity android:name=".activities.recipeActivities.DetailedRecipeTypeActivity" />
        <activity android:name=".activities.recipeActivities.ListViewRecipeTypeActivity" />
        <activity android:name=".activities.loginActivities.LoginActivity" />
        <activity android:name=".activities.loginActivities.SignUpActivity" />
    </application>
</manifest>